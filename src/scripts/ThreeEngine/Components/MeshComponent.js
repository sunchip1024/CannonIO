/*
import * as THREE from "three";
import Component from "./Component.js";
import { CheckType } from "../Utils/Utils";

export default class MeshComponent extends Component {
    constructor(mesh) {
        CheckType(mesh, THREE.Mesh);
        super();
        this.mesh = mesh;
    }
}
*/